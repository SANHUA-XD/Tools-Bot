Font files were intentionally left OUT of this zip (they get corrupted when
zipped in this environment). Copy your existing .ttf/.otf files from your
current deployment back into this folder before deploying - nothing in the
code changed, so your existing font files will work as-is.
